# PR Follow-ups and Technical Debt

This file lists follow-up tasks and locations found in the codebase after preparing
`fix/tests-backend-2026-04-16`.

## 1) Vectorize uses of `map_elements`
Files and example lines:

- src/transformacao/movimentacao_estoque_pkg/c170_xml.py (lines with `.map_elements`)
- src/transformacao/movimentacao_estoque_pkg/mapeamento_fontes.py
- src/transformacao/rastreabilidade_produtos/fatores_conversao.py
- src/transformacao/calculos_periodo_pkg/calculos_periodo.py
- src/transformacao/calculos_mensais_pkg/calculos_mensais.py
- src/interface_grafica/services/aggregation_service.py
- src/transformacao/calculos_anuais_pkg/calculos_anuais.py

Recommendation: replace `map_elements` with native Polars string/struct expressions
or compute derived columns once and use `partition_by(..., as_dict=True)` for lookups.

## 2) Replace deprecated `str.concat` with `str.join`
Occurrences:

- src/transformacao/calculos_anuais_pkg/calculos_anuais.py: `.sort().str.concat(";").alias("ST")`
- src/transformacao/calculos_mensais_pkg/calculos_mensais.py: `.sort().str.concat(";").alias("ST")`

Action: change to `.sort().str.join(";").alias("ST")` and run tests.

## 3) Tests returning values (PytestReturnNotNoneWarning)
Files with `return` usages in tests (manual inspection recommended):

- tests/test_estoque_inicial_final_anual.py (several `return True` inside tests)
- tests/test_aux_classif_merc.py (helper returns within test scope)
- other files shown during grep search (see git history / grep results)

Action: inspect tests that `return` non-None and replace with `assert` where appropriate.

## 4) Misc cleanup
- Remove any remaining temporary debug files if present.
- Re-run full test suite in CI (GitHub Actions) and add CI notes to PR (I can prepare a checklist).

## Next steps I can take (pick one or more):
- A) Create individual PRs to vectorize `map_elements` usages file-by-file.
- B) Replace `str.concat` → `str.join` in a single focused PR.
- C) Fix tests returning values (small follow-up PR).
- D) Prepare a CI checklist and notes to include in PR description.

---
Generated by the automation agent during code review.

## PR #95 — fix(agregacao,fatores): deterministic ids_origem_agrupamento and avoid Expr.apply

- **URL:** https://github.com/Enio-Telles/audit_pyside/pull/95
- **Branch:** fix/agregacao-fatores-conversao
- **Summary:** Preserve deterministic ordering for `ids_origem_agrupamento` (use user-selected order) and replace invalid `pl.Expr.apply` usage in `fatores_conversao.py` with a safe, per-row materialized processing.
- **Files changed:**
	- `src/interface_grafica/services/aggregation_service.py`
	- `src/transformacao/rastreabilidade_produtos/fatores_conversao.py`
- **Tests (local):** 169 passed, 3 warnings
- **Reviewers requested:** Enio-Telles (note: cannot request review from PR author)
- **Labels applied:** bug, fix, tests-passed
- **Notes:** The PR uses a materialized per-row processing which may affect memory/performance on very large datasets. Consider a follow-up to vectorize the change.

---

Actions performed:
- Added labels `bug`, `fix`, `tests-passed` to PR #95.
- Attempted to request review from `Enio-Telles`, but review requests cannot be made to the PR author; please specify an alternate reviewer if desired.
- Committed and pushed this document update to branch `fix/agregacao-fatores-conversao` so the PR contains the follow-up notes.

